--- a/models/qwen3_8_flash_next/common/qsa_cache.py
+++ b/models/qwen3_8_flash_next/common/qsa_cache.py
@@ -14,6 +14,7 @@
 from __future__ import annotations
 
 import math
+import os
 from dataclasses import dataclass
 from functools import cache
 from typing import ClassVar
@@ -582,6 +583,13 @@
         else:
             self.compress_ratio = 1
         self.storage_block_size = kv_cache_spec.storage_block_size
+        # Fused multi-step draft decode: every build() input is a persistent
+        # buffer that the speculator advances in place between draft steps, and
+        # every scalar argument is constant across those steps. So a refresh is
+        # one more launch of the same metadata kernel into the same buffers.
+        self.supports_draft_decode_metadata_update = (
+            os.environ.get("VLLM_QSA_FUSED_DRAFT", "0") == "1"
+        )
         max_tokens = vllm_config.scheduler_config.max_num_batched_tokens
         self.token_to_req_buffer = torch.empty(
             max_tokens, dtype=torch.int32, device=device
@@ -624,11 +632,7 @@
                 num_tokens + (self.compress_ratio - 1) * num_requests
             ) // self.compress_ratio
             k_work_metadata = self.k_work_metadata_buffer[:max_num_work]
-        token_to_req, logical_positions, slot_mapping = build_qsa_metadata(
-            common_attn_metadata,
-            self.token_to_req_buffer,
-            self.logical_positions_buffer,
-            self.slot_mapping_buffer,
+        rebuild_kwargs = dict(
             storage_block_size=self.storage_block_size,
             compress_ratio=self.compress_ratio,
             circular_buffer_size=(
@@ -637,6 +641,15 @@
             k_work_metadata_buffer=k_work_metadata if build_k_work else None,
             request_capacity=request_capacity,
         )
+        token_to_req, logical_positions, slot_mapping = build_qsa_metadata(
+            common_attn_metadata,
+            self.token_to_req_buffer,
+            self.logical_positions_buffer,
+            self.slot_mapping_buffer,
+            **rebuild_kwargs,
+        )
+        if self.supports_draft_decode_metadata_update:
+            self._draft_rebuild = (common_attn_metadata, rebuild_kwargs)
         return QSAForwardMetadata(
             block_table=common_attn_metadata.block_table_tensor,
             slot_mapping=slot_mapping,
@@ -650,6 +663,25 @@
             compress_ratio=self.compress_ratio,
         )
 
+    def update_draft_decode_metadata(self, metadata: QSAForwardMetadata) -> None:
+        """Refresh step-dependent QSA metadata in place for the next draft step.
+
+        Re-launches the metadata kernel with the arguments of the last build().
+        Its inputs (query_start_loc, seq_lens, slot_mapping, block table) are the
+        persistent buffers the speculator advanced in place, and its outputs are
+        this builder's persistent buffers, which `metadata` already references.
+        No host synchronization, so the call is safe inside CUDA graph capture.
+        """
+        del metadata
+        common_attn_metadata, rebuild_kwargs = self._draft_rebuild
+        build_qsa_metadata(
+            common_attn_metadata,
+            self.token_to_req_buffer,
+            self.logical_positions_buffer,
+            self.slot_mapping_buffer,
+            **rebuild_kwargs,
+        )
+
 
 class QSAStateBackend(AttentionBackend):
     """Key-only dummy backend for out-of-band BF16 QSA side-cache operations."""
