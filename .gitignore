node_modules
dist
.env
.env.*
*.log
# Only the CLI launcher script is tracked. The other bin/ entries are build
# outputs (native binaries and libpulse_engine.so); build them with `make`.
bin/*
!bin/pulse-cli
*.gguf
packages/**/*.gguf
build/
__pycache__/
*.py[cod]
*.gguf
**/*.gguf

# Node and gateway configuration with real hosts, IPs and paths for this
# deployment. Copy the matching *.example.json file and edit it in place.
runtime/nodes.json
config/qwen38-gateway.json
